<fieldset>
  <%= select_tag :rolesignature_id, options_from_collection_for_select(Role.all, :id, :name, :id)%>

  <%= f.hidden_field :_destroy %>
  <%= link_to "remove", '#', class: "remove_fields" %>
</fieldset>
