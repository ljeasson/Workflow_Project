<!DOCTYPE html>

<html>
  <head>
    <title>Workflow App</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <link href="jumbotron.css" rel="stylesheet">
  </head>
  <body>
    <nav class="navbar navbar-custom bg-faded">

      <div class="navbar-header">
        <a class="navbar-brand">Project Workflow</a>
      </div>

      <ul class="nav navbar-nav">
        <li class="active"><%= link_to 'Home', root_path%></li>
        <li class="active"><a href="/forms/">Forms</a></li>
        <li class="active"><a href="/form_types/">Form Templates</a></li>
      </ul>
        <div align = right>
          <% if current_user %>
            <font color ="white">Signed in as </font><strong style = color:white><%= current_user.name %></strong>!
            <%= link_to "Sign out", signout_path, class: "btn btn-primary", id: "sign_out" %>
          <% else %>
            <!-- Commented this out: <%= link_to "Sign in with Google", "/auth/google_oauth2", class: "btn btn-primary", id: "sign_in" %> -->
          <% end %>
        </div>
      </nav>
    <div>
    <%= yield %>
    </div>
  </body>


</html>
